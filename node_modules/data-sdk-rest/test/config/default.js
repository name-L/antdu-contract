const tokenRestUrl = "https://antfact.com/auth2_test";
const authRestUrl = "https://antfact.com/auth2_test";
const baseRestUrl = "https://test.antfact.com/eageye/v4";

module.exports = {

    "oauth": {
        "appKey": "3E6570bKX4okbBT10TANVohs",
        "appSecret": "349lUwapX54MbjA0wM5q0ZiS",
        "forceUserOutErrorCodes": ["11023", "11045", "11042", "11046", "11048", "11021", "11043"],
        "tokenServerUrl": tokenRestUrl + "/authz/token"
    },
    "user": {
        "loginUrl": authRestUrl + "/authc/login",
        "logoutUrl": authRestUrl + "/authc/logout",
        "userAppConfigUrl": baseRestUrl + "/user/system_conf"
    },
    "system": {
        "elapseLogMinTime": 0,
        "sdkRestTimeout": 1000 * 30,

        "eureka": {
            "aid": {
                "push": "TESTCOORDIANTOR"
            },
            // 测试
            // "host": "172.19.103.51",
            // "port": 80
            // 正式
            "host": "172.19.103.22",
            "port": 8888
        },

        "local": {
            "basedir": `${__dirname}/local-lang`,
            "supported": ["zh-CN"],
            "customImplGetLocalLangStrByRequest": (request) => {
                // 1. 优先使用 用户个人设置中的语言选择
                let lang = (request.session && request.session.user && request.session.user.appConfig) ? request.session.user.appConfig.lang : null;
                // 2. 使用 cookies 中的语言选择
                if (!lang) {
                    lang = request.cookies["lang"];
                }
                return lang;
            }
        },
        "errorCode": {
            "basedir": `${__dirname}/error-code`
        }

    }

};