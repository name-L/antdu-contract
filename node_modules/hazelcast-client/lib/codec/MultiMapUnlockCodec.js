"use strict";
var ClientMessage = require("../ClientMessage");
var BitsUtil_1 = require("../BitsUtil");
var MultiMapMessageType_1 = require("./MultiMapMessageType");
var REQUEST_TYPE = MultiMapMessageType_1.MultiMapMessageType.MULTIMAP_UNLOCK;
var RESPONSE_TYPE = 100;
var RETRYABLE = false;
var MultiMapUnlockCodec = (function () {
    function MultiMapUnlockCodec() {
    }
    MultiMapUnlockCodec.calculateSize = function (name, key, threadId) {
        var dataSize = 0;
        dataSize += BitsUtil_1.BitsUtil.calculateSizeString(name);
        dataSize += BitsUtil_1.BitsUtil.calculateSizeData(key);
        dataSize += BitsUtil_1.BitsUtil.LONG_SIZE_IN_BYTES;
        return dataSize;
    };
    MultiMapUnlockCodec.encodeRequest = function (name, key, threadId) {
        var clientMessage = ClientMessage.newClientMessage(this.calculateSize(name, key, threadId));
        clientMessage.setMessageType(REQUEST_TYPE);
        clientMessage.setRetryable(RETRYABLE);
        clientMessage.appendString(name);
        clientMessage.appendData(key);
        clientMessage.appendLong(threadId);
        clientMessage.updateFrameLength();
        return clientMessage;
    };
    return MultiMapUnlockCodec;
}());
exports.MultiMapUnlockCodec = MultiMapUnlockCodec;
//# sourceMappingURL=MultiMapUnlockCodec.js.map